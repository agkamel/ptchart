% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pt_funs.R
\name{b1}
\alias{b1}
\alias{b0}
\alias{celeration}
\alias{celeration_0}
\alias{bounce_up}
\alias{bounce_down}
\alias{bounce_total}
\alias{accuracy}
\title{Vectorized functions for summary PT measures}
\usage{
b1(x, y)

b0(x, y)

celeration(x, y)

celeration_0(x, y)

bounce_up(x, y)

bounce_down(x, y)

bounce_total(x, y)

accuracy(x, y_cor, y_incor)
}
\arguments{
\item{x}{Integer. Day of calendar}

\item{y}{Double. Frequency}

\item{y_cor}{Double. Correct frequency}

\item{y_incor}{Double. Incorrect frequency}
}
\value{
A single value as a double scalar.
}
\description{
These funtions are summary PT measures that returns only a single value.
}
\section{Functions}{
\itemize{
\item \code{b1()}: Calculate b1

\item \code{b0()}: Calculate b0

\item \code{celeration()}: Calculate celeration

\item \code{celeration_0()}: Calculate celeration intercept

\item \code{bounce_up()}: Calculate bounce up

\item \code{bounce_down()}: Calculate bounce down

\item \code{bounce_total()}: Calculate bounce total

\item \code{accuracy()}: Calculate accuracy

}}
\section{Slope}{
The slope \eqn{b_1} obtained with \code{b1()} is calculated with

\deqn{\frac{\sum (x - \bar{x})(\log_{10}(y) - \log_{10}(\bar{y}))}{\sum(x - \bar{x})^2}}

where \eqn{x} is the day and \eqn{y} is the frequency.
}

\section{Intercept}{
The intercept \eqn{b_0} obtained with \code{b0()} is calculated with

\deqn{\log_{10}(\bar{y}) - b_1 \bar{x}}
}

\section{Predicted values}{
The predicted values \eqn{\log_{10}(\hat{y})} obtained with \code{predicted_values()} is calculated with

\deqn{b_0 + b_1 x}
}

\section{Residuals}{
The residual errors \eqn{\epsilon} obtained with \code{res()} is calculated with

\deqn{\log_{10}(y) - \log_{10}(\hat{y})}
}

\section{Celeration}{
The celeration \eqn{\mathcal{C}} obtained with \code{celeration()} is calculated with

\deqn{(10^{b_1})^7}
}

\section{Accuracy ratios}{
The accuracy ratios \eqn{a} obtained with \code{accuracy_ratio()} is calculated with

\deqn{\frac{y_{\text{correct}}}{y_{\text{incorrect}}}}
}

\section{Accuracy}{
The accuracy \eqn{\mathcal{A}} obtained with \code{accuracy()} is calculated with

\deqn{(10^{(\frac{\sum (x - \bar{x})(a - \bar{a})}{\sum(x - \bar{x})^2})})^7}
}

\section{Bounce up}{
The bounce up \eqn{\mathcal{B}_{\text{up}}} obtained with \code{bounce_up()} is calculated with

\deqn{10^{\max(\epsilon)}}
}

\section{Bounce down}{
The bounce down \eqn{\mathcal{B}_{\text{down}}} obtained with \code{bounce_down()} is calculated with

\deqn{10^{\min(\epsilon)}}
}

\section{Bounce total}{
The bounce total \eqn{\mathcal{B}_{\text{total}}} obtained with \code{bounce_total()} is calculated with

\deqn{\mathcal{B}_{\text{up}} \mathcal{B}_{\text{down}}}
}

\examples{
# NOT YET

}
