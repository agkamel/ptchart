% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ptstat.R
\name{ptstat}
\alias{ptstat}
\alias{print.ptstat}
\title{ptstat}
\usage{
ptstat(
  data,
  day = NULL,
  freq = NULL,
  phase = NULL,
  date = NULL,
  date_zero = NULL,
  count = NULL,
  time = NULL,
  count_err = NULL,
  freq_err = NULL,
  count_floor = NULL,
  count_ceil = NULL,
  verbose = TRUE
)

\method{print}{ptstat}(x, ...)
}
\arguments{
\item{data}{Dataframe.}

\item{day}{Numeric. No of the successive day of the calendar. Values must be integers and >= 0.}

\item{freq}{Numeric. Frequencies of observations aimed to be accelerated. Values must be >= 0.}

\item{phase}{Charactor. Phase of intervention. If none are provided, default phase is \code{"A"} for all observations. Note: Providing factor vector to be implemented.}

\item{date}{Date. Dates of observations in the format "yyyy-mm-dd". If \code{date_zero} is not provided, the closest sunday before the first date will be used for \code{date_zero}.}

\item{date_zero}{Date of length one. A date that correspond to day 0 in the format "yyyy-mm-dd".}

\item{count}{Integer. Count of responses aimed to be accelerated. Values must be >= 0.}

\item{time}{Numeric. Number of minutes. Values must be > 0.}

\item{count_err}{Integer. Count of responses aimed to be decelerated. Values must be >= 0.}

\item{freq_err}{Numeric. Frequencies of observations aimed to be decelerated. Values must be >= 0.}

\item{count_floor}{Double. NOTE. Name may be changed...}

\item{count_ceil}{Double. NOTE. Name may be changed...}

\item{verbose}{Logical. Show informational messages. Default to \code{TRUE}.}

\item{x}{An S3 object of class ptstat.}

\item{...}{Additional arguments.}
}
\value{
An S3 object of class \code{ptstat}, a list of computation tables.
}
\description{
Main function for computing main measures of behavior change.
}
\details{
\code{ptchart()} returns a list that contains all calculations.
More specifically:
\itemize{
\item \code{arg_table} is a data frame that contains the call values of the function.
\item \code{pttable} is a data frame that contains all basic calculations like frequencies, logged frequencies, accuracy ratios, etc. This table can be extracted with \code{pttable()}.
\item \code{pttables} is a list of data frames with same columns of \code{pttables} except they are splitted by phase.
\item \code{desc} contains descriptives analysis.
\item \code{terms} contains terms of a regression model.
\item \code{a_table} contains accuracy values (or Accuracy Improvement Measure). This table can be extracted with \code{accuracy()}.
\item \code{c_table} contains celeration values. This table can be extracted with \code{celeration()}.
\item \code{b_table} contains bounce values. This table can be extracted with \code{bounce()}, \code{bounce_up()}, \code{bounce_down()} or \code{bounce_total()}.
\item \code{j_table} contains jump values. This table can be extracted with \code{jump()}.
\item \code{t_table} contains turn values. This table can be extracted with \code{turn()}.
}
}
\section{Methods (by generic)}{
\itemize{
\item \code{print(ptstat)}: Print method for ptstat

}}
\examples{
ptstat(example_pt_data,
       day = "jour",
       count = "reponse",
       time = "minute",
       phase = "phase"
)
}
